#+TITLE: Documentation Technique : Vue Historique (views/history.py)
#+AUTHOR: Agentic Engineering Team
#+DATE: [2026-02-22]

* Rôle de la Vue
Affiche la liste chronologique inverse (le plus récent en haut) de toutes les transactions enregistrées dans le fichier CSV.

* Implémentation
On parcourt le DataFrame inversé (=iloc[::-1]=) et on transforme chaque ligne en un =ft.ListTile=. On utilise un code couleur (Vert/Rouge) pour distinguer visuellement les revenus des dépenses.

#+begin_src python :tangle ../views/history.py
import flet as ft
import database

def get_history_view():
    df = database.get_df()
    items = []
    for _, row in df.iloc[::-1].iterrows():
        is_income = row['Type'] in ["Income", "Miditra"]
        items.append(
            ft.ListTile(
                leading=ft.CircleAvatar(
                    content=ft.Icon(ft.Icons.MONEY, color=ft.Colors.WHITE),
                    bgcolor=ft.Colors.GREEN if is_income else ft.Colors.RED,
                ),
                title=ft.Text(row['Category'], weight="bold"),
                subtitle=ft.Text(row['Date']),
                trailing=ft.Text(
                    f"{' + ' if is_income else ' - '} Ar {row['Amount']:,.0f}",
                    color=ft.Colors.GREEN if is_income else ft.Colors.RED,
                    weight="bold"
                ),
            )
        )
    return ft.Column([
        ft.Container(
            content=ft.Text("Tantara", size=24, weight="bold"),
            padding=20
        ),
        ft.Column(items, scroll=ft.ScrollMode.AUTO, expand=True)
    ], expand=True)
#+end_src
